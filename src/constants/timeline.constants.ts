export const DEFAULT_PIXELS_PER_DAY = 40;
export const LEFT_PANEL_WIDTH = 200; // px
export const ROW_HEIGHT = 56; // px

export const PIXELS_PER_DAY_BY_VIEW_MODE = {
  day: 40,
  week: 80,
  month: 120
};

export const MIN_TASK_WIDTH = 40; // Minimum width in pixels for a task bar

